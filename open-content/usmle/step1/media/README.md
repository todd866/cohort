# Open Step 1 media

Rights-cleared figures for `open-content/usmle/step1` questions.

| File | Role |
|---|---|
| `<assetId>.svg` (etc.) | Source asset bytes (FOSS redistributable) |
| `<assetId>.kernel.json` | FigureKernel brief used to author the asset |

Serving copies may also live under `public/figures/usmle/step1/` with a
matching `figure-sidecars/` entry. The open-content copy is the provenance
source of truth.

**Product grind home:** `docs/programs/cohort.md/` (FIGURE_GRIND + ledger).  
Contract: `docs/designs/2026-08-04-usmle-figure-lane.md`.
